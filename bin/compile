#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

echo "Test ?"
ruby -v
curl -s --retry 3 -L https://developer.salesforce.com/media/salesforce-cli/sfdx-v5.7.6-d42cf65-linux-amd64.tar.xz  | tar -xvf
./sfdx/install
